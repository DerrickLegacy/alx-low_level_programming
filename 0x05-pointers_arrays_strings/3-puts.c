#include "main.h"
#include <stdio.h>

/**
 * _puts - Prints the string literal passed in.
 * @str: The input to our function.
 * Return: Returns the printed string .
 */

void _puts(char *str)
{
	puts(str);
}
