#include "main.h"

/**
 * _puts - writes a string to stdout not including null character
 * @s: sttring to be written
 * Return: Successful if a non-negative value is returned, EOF for an error
 */
void _puts(char *s)
{

}
